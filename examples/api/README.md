# Noir Api Testing Example

This project contains a very simple HTTP API which exposes one endpoint which 
is a proxy for https://icanhazip.com . 

It integrates noir to test the functionality of this endpoint.

